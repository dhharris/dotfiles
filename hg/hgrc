[extensions]
# Built-ins
convert =
histedit =
progress =
purge =
rebase =

# hg-experimental (Facebook): https://bitbucket.org/facebook/hg-experimental
# mkdir .hgext && cd .hgext && hg clone https://bitbucket.org/facebook/hg-experimental
amend =
smartlog = ~/.hgext/hg-experimental/hgext3rd/smartlog.py

# Hg-git extensions: `easy_install hg-git`
# requires setuptools and cython (pip packages)
bookmarks =
hggit =

# Evolve extension: `pip install --user hg-evolve`
#evolve =

[ui]
# Merge by manually editing the files (like in FB)
merge = internal:merge3 
username = Hugh Harris <dhharris9@gmail.com>
